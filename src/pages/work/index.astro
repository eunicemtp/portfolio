---
// src/pages/work.astro
import Layout from '../../layouts/Layout.astro';
import projets from '../../data/projets.json';
import { translations } from '../../data/lang.js';
import ProjectCard from '../../components/CardProjet.astro';
import Project from '../../components/Work.astro';

type Lang = 'en' | 'fr';

const { lang } = Astro.props as { lang: Lang };
const pageKey = 'work';
const t = translations[lang] || translations['en'];
const filtres = [
  t.content.work.all,
  t.content.work.web,
  t.content.work.poster,
  t.content.work.hobby
];


---

<Layout title={t.title[pageKey]} pageClass={`${lang} ${pageKey}`}>

    <Project lang={lang}/>
</Layout>
