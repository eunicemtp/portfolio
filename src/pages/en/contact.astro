---

import Layout from '../../layouts/Layout.astro';
import projets from '../../data/projets.json';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Contactme from '../../components/Contact.astro';
import { translations } from '../../data/lang.js';

import '../../styles/global.css';
const lang = 'en'; // ou 'fr'
const pageKey = 'contact';
const t = translations[lang];
---





<Layout title={t.title[pageKey]} pageClass={`${lang} ${pageKey}`}>
    <Contactme lang={lang}/>
    
</Layout>

<!-- <form action="https://formspree.io/f/TON_ID" method="POST" class="space-y-4 max-w-md">
  <input type="text" name="name" required placeholder="Votre nom" class="w-full border p-2 rounded">
  <input type="email" name="email" required placeholder="Votre email" class="w-full border p-2 rounded">
  <textarea name="message" required placeholder="Votre message" class="w-full border p-2 rounded h-32"></textarea>
  <button type="submit" class="bg-mint text-white px-4 py-2 rounded">Envoyer</button>
</form> -->